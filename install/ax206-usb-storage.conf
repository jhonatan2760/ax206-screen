# A telinha se apresenta como pendrive; sem isto o kernel liga o usb-storage nela.
# Copie para /etc/modprobe.d/. Para aplicar sem reboot:
#   echo 1908:0102:i | sudo tee /sys/module/usb_storage/parameters/quirks
options usb-storage quirks=1908:0102:i
