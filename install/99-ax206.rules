# Telinha AX206 (firmware dpf-ax, "USB-Display"): acesso ao usuario logado sem root.
# Copie para /etc/udev/rules.d/ e rode: sudo udevadm control --reload-rules && sudo udevadm trigger
SUBSYSTEM=="usb", ATTR{idVendor}=="1908", ATTR{idProduct}=="0102", MODE="0660", TAG+="uaccess"
